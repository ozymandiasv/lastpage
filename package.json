{
  "name": "last-page",
  "version": "1.0.0",
  "description": "Last Page — a lightweight, Git-powered static publishing platform. No required build dependencies.",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "admin": "node admin/server.js",
    "serve": "node scripts/serve.js"
  },
  "optionalDependencies": {
    "sharp": "^0.33.0"
  },
  "engines": {
    "node": ">=18"
  }
}
